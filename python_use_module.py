# use the module via the import keyword
import python_modules # custom module
import platform # example of an inbuilt moduel
python_modules.greeting(python_modules.person["name"])

x = platform.system()
x = dir(platform)
print(x)